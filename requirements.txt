# Core Hardware-Splicer compiler/product runtime (lightweight profile).
# Keep this aligned with pyproject.toml so `make setup` exercises the shipped API.
fastapi>=0.110
uvicorn>=0.27
httpx>=0.27
pillow>=10.0
pydantic>=2.0
pydantic-settings>=2.0
python-multipart>=0.0.9
python-dotenv>=1.0
loguru>=0.7
pytest>=8.0

# Optional — only needed for true STL/CadQuery rendering via 3D-Splicer
# cadquery
