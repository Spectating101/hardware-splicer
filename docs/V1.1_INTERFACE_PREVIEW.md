# v1.1 interface preview — release boundary

**Status:** `main` branch · **not** `v1.0.2` · internal preview name: **v1.1-interface-preview**

---

## Version map

```text
v1.0.2 (tag)     = stable internal-maturity / pilot SKU proof
main (HEAD)      = v1.1-interface-preview — OSS adapter layer emerging
```

Do **not** fold v1.1 interface claims into v1.0.2 release copy or GitHub Release assets without a new tag and re-verification.

---

## Product category (v1.1)

> **Self-hosted auditable hardware bring-up + design verification workbench** with an emerging OSS interface layer.

Not a bring-up checklist alone. Not a browser ECAD suite.

---

## v1.1 preview scope (what ships on `main`)

| Surface | What it proves |
|---------|----------------|
| **Design tab** | KiCanvas preview → compile truth → BOM → fab coverage → exports |
| **Wiring tab** | Topology Mermaid + narrative guide |
| **Interface lab** | Adapter proving ground: canvas, circuit-json, KiCad netlist paste |
| **Authority layer** | `PROJECT_PACKAGE`, DRC, bench gates, power-on boundary (unchanged moat) |

---

## Out of scope for v1.1 preview

- Native Windows installer
- Full tscircuit editor embed
- Production autoroute as default UX
- KiCad MCP as hard runtime dependency
- Public SaaS
- More competitor/strategy docs

---

## Verification bar (before tagging `v1.1.0-alpha.1`)

```bash
make verify-product-v1
make verify-ui-interface-smoke    # API on :8787
pytest tests/test_build_files_security.py tests/test_oss_integrations_api.py -q
```

Manual: Home → Quick demo → **Design verify** (stepper, KiCanvas, BOM, fab) → Interface lab compile → recent build reload.

**Release-candidate commit:** `e4e75f5` (interface UX polish + UI smoke).

---

## Next after alpha tag (ordered)

1. External proof / pilot conversations ([`EXTERNAL_PROOF_CHECKLIST.md`](EXTERNAL_PROOF_CHECKLIST.md))
2. JLC enrich toggle in UI
3. Mermaid offline fallback

---

## Related

- [`RELEASE_NOTES_v1.1.0-alpha.1.md`](../RELEASE_NOTES_v1.1.0-alpha.1.md) (draft)
- [`BUILD_FILES_API_SECURITY.md`](BUILD_FILES_API_SECURITY.md)
- [`CLAIMS_BOUNDARY.md`](CLAIMS_BOUNDARY.md)
- [`OSS_INTEGRATION_STATUS.md`](OSS_INTEGRATION_STATUS.md)
