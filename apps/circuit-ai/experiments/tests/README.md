# Experiments / Ad-hoc Tests

This folder holds one-off scripts that were used to validate or prototype features.

Real automated tests should live under `tests/` (if present) and be runnable via a test runner.
